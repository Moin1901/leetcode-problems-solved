class Solution {
public:
    int fib(int n) {
        int prev,prev2,curr;
        if(n<=1)return n;
        prev2=0;
        prev=1;
        for(int i=2;i<=n;i++){
        curr=prev2+prev;
        prev2=prev;
        prev=curr;

        }
        return prev;
        
    }
};
